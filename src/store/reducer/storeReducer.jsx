const storeStateInitial = {
    storeInfo: "",
  };
  
  const storeReducer = (state = storeStateInitial, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  export default storeReducer;
  